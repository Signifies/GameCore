# Implement tier system into the permission API used in the build system??
# Once we begin build implementation most of the methods from Utils will be depreicated and taken from builds API...
# implement tier system on debug status.
# For chat event look at how we are going to handle this....
  most games will be built with GameCore as a depandancy, but we must check to see where chat will be handled.
